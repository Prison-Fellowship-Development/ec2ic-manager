# AWS Connect - Python Requirements
# 
# The application uses only Python standard library modules:
# - tkinter (GUI)
# - subprocess (running AWS CLI commands)
# - threading (async operations)
# - json, os, sys, random, configparser, time, pathlib, platform
#
# No additional packages are required to run the application.
#
# Build dependencies (installed automatically by build_macos.sh):
# - pyinstaller (for building the macOS app)
# - create-dmg (for creating the installer, via Homebrew)
